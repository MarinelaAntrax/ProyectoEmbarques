<nav class="bae-header navbar-default" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-2">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>

            <a href="@Url.Action("Index", "EnsamblesRealizados")"><h2 class="bae-title"> EMBARQUES </h2></a>
        </div>

        <div class="collapse navbar-collapse bae-area-menu" id="bs-example-navbar-collapse-2">
            @if (HttpContext.Current.User.Identity.IsAuthenticated&& (User.IsInRole("AppAdminEMBARQUES") || User.IsInRole("IT")))
            {
                    <ul class="nav navbar-nav">
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown"> Agregar <b class="caret"></b></a>
                            <ul class="dropdown-menu">
                                <li><a href="@Url.Action("Create", "Shipping_Records")"><i class="fa fa-navicon" aria-hidden="true"></i>&nbsp;Embarque de Ensambles</a></li>
                            </ul>
                        </li>
                    </ul>

                    <ul class="nav navbar-nav">
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown"> Catalogo <b class="caret"></b></a>
                            <ul class="dropdown-menu">
                                <li><a href="@Url.Action("Index", "Clientes")"><i class="fa fa-users" aria-hidden="true"></i>&nbsp;Ver Clientes</a></li>
                                <li><a href="@Url.Action("Index", "Ensambles")"><i class="fa fa-wrench" aria-hidden="true"></i>&nbsp;Ver Ensambles</a></li>
                            </ul>
                        </li>
                    </ul>
             }
                <p class="nav navbar-text navbar-right"><i class="fa fa-lg fa-user-circle"></i>&nbsp;¡Hola, @User.Identity.Name!</p>
        </div>
    </div>
</nav>